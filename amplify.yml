version: 1
backend:
  phases:
    preBuild:
      commands:
        - rm -rf node_modules
        - rm -rf .next
        - rm -rf node_modules/.prisma
        - rm -rf node_modules/@prisma
        - npm install
        - npx prisma generate

    build:
      commands:
        - npm run build

  artifacts:
    baseDirectory: .next
    files:
      - "**/*"

  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
